# First User Decision

The first public user path should prove local install, source diagnostics, and
report generation using sanitized sample traces.

This note intentionally avoids sensitive filesystem locations or machine-specific evidence.
